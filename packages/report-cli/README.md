# @quality-report/report-cli

Static quality-report generator for Node 22 and newer.

```text
npx quality-report --version
npx quality-report validate --config quality-report.yml --input quality-artifacts
```

Install this package together with the matching release-candidate versions of
`@quality-report/report-core` and `@quality-report/adapters`.
